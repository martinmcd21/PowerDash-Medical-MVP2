# OpenAI API key used by the OpenAI Python SDK
OPENAI_API_KEY="your_api_key_here"

# Optional: set a default model (the UI still lets you override at runtime)
# POWERDASH_MEDICAL_DEFAULT_MODEL="gpt-5.2"
